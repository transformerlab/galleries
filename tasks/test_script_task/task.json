{
  "name": "test-script-remote",
  "description": "Example remote task that uses transformerlab-sdk test_script and uploads its directory.",
  "type": "REMOTE",
  "plugin": "remote_orchestrator",
  "inputs": {},
  "outputs": {},
  "config": {
    "command": "python -m transformerlab_sdk.test_script",
    "notes": "This is a sample command; replace with appropriate entrypoint on remote machine."
  },
  "upload_dir": "sample script" 
}

