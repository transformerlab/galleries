{
  "name": "test-script-remote",
  "description": "Example remote task that uses transformerlab-sdk test_script and uploads its directory.",
  "type": "REMOTE",
  "plugin": "remote_orchestrator",
  "inputs": {},
  "outputs": {},
  "config": {
    "command": "python src/test_script.py",
    "notes": "Files from the task root are staged into a temporary src/ before upload."
  }
}

